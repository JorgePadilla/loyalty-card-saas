<% content_for(:title, "Edit Campaign") %>
<div class="mb-6">
  <h1 class="text-2xl font-semibold text-matte">Edit Campaign</h1>
  <p class="text-secondary text-sm mt-1"><%= @campaign.name %></p>
</div>
<div class="max-w-2xl bg-surface border border-border rounded-xl p-6">
  <%= render "form", campaign: @campaign %>
</div>
