<% content_for(:title, "Edit Reward") %>

<div class="mb-6">
  <h1 class="text-2xl font-semibold text-matte">Edit Reward</h1>
  <p class="text-secondary text-sm mt-1"><%= @reward.name %></p>
</div>

<div class="max-w-2xl bg-surface border border-border rounded-xl p-6">
  <%= render "form", reward: @reward %>
</div>
