<% content_for(:title, t("admin.users.new.title")) %>
<div class="mb-6">
  <h1 class="text-2xl font-semibold text-matte"><%= t("admin.users.new.title") %></h1>
  <p class="text-secondary text-sm mt-1"><%= t("admin.users.new.subtitle") %></p>
</div>
<div class="max-w-2xl bg-surface border border-border rounded-xl p-6">
  <%= render "form", user: @user %>
</div>
