import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App placeholder test', (WidgetTester tester) async {
    // Placeholder test — real tests to come
    expect(1 + 1, 2);
  });
}
